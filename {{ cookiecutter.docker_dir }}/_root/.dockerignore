# Exclude everything but what is strictly needed for building images.
*
!docker/**/docker-entrypoint.sh

# Required for python apps installations.
!poetry.lock
!pyproject.toml

# Required for node apps installations.
!package.json
!package-lock.json
